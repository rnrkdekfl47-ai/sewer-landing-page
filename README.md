# 맑은물배관 랜딩페이지

하수구 막힘·누수 전문업체 랜딩페이지. 순수 정적 HTML(빌드 불필요)로, Vercel에 바로 배포됩니다.

## 로컬에서 보기

`index.html` 파일을 브라우저로 열면 됩니다.

## 배포

Vercel에서 이 저장소를 Import하면 별도 설정 없이 정적 사이트로 배포됩니다 (Framework Preset: Other / Root Directory: `./`).
